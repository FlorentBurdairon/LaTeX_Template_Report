% List of packages
% (***) : deactivate if using BEAMER
% Definitions of packages come from CTAN

%================================================================
% Overview
%================================================================
% Accept different input encodings
% Note : encoding to take into account the accents
\usepackage[utf8]{inputenc}
% Standard package for selecting font encodings
% Note : for "beautiful" accents
\usepackage[T1]{fontenc}
% Multilingual support for Plain TeX or LaTeX
% Note : adaptation to french
\usepackage[french]{babel}
% Context sensitive quotation facilities (because of babel)
\usepackage{csquotes}
% Package Latin Modern : package of fonts
\usepackage{lmodern}
% METAFONT and macros for Euro sign
\usepackage{eurosym}
% Reimplementation of and extensions to LaTeX verbatim
% Note : environment to include unformatted text (ex: raw code)
\usepackage{verbatim}
% Control layout of itemize, enumerate, description
\usepackage{enumitem}
% Extensive support for hypertext in LaTeX
\usepackage{hyperref}
% A new bookmark (outline) organization for hyperref
\usepackage{bookmark}
% Standard LaTeX package for creating indexes
\usepackage{makeidx}
%\makeindex
% Produce lists of symbols as in nomenclature
\usepackage[french]{nomencl}
%\makenomenclature
% LaTeX support for the Text Companion fonts
\usepackage{textcomp}
% Driver-independent color extensions for LaTeX and pdfLaTeX
\usepackage{xcolor}
% Coloured boxes, for LaTeX examples and theorems, etc
\usepackage{tcolorbox}
%\usepackage{pdfpages}

%================================================================
% Bibliography
%================================================================
% Sophisticated Bibliographies in LaTeX
\usepackage[backend=biber,style=numeric, citestyle=numeric]{biblatex}
%\usepackage{biblatex}
% Full bibliography entries in the main text of a document
\usepackage{bibentry}

%================================================================
% Layout of the document
%================================================================
% Flexible and complete interface to document dimensions (***)
\usepackage[top=2cm, bottom=2cm, left=2cm, right=2cm]{geometry}
% Select alternative section titles (***)
\usepackage{titlesec} 
% Add bibliography/index/contents to Table of Contents (***)
\usepackage{tocbibind}	%[nottoc, notlof, notlot]
% Extensive control of page headers and footers in LaTeX
\usepackage{fancyhdr}
% Extra control of appendices
\usepackage{appendix}
% Intermix single and multiple columns
\usepackage{multicol}

%================================================================
% Mathematics
%================================================================
% AMS mathematical facilities for LaTeX : extension package for LaTeX that provides various features to facilitate writing math formulas and to improve the typographical quality of their output
\usepackage{amsmath}
% List of mathematical symbols
\usepackage{amssymb}
% TeX fonts from the American Mathematical Society : an ex­tended set of fonts for use in math­e­mat­ics
\usepackage{amsfonts}
% Mathematical tools to use with amsmath
\usepackage[fixamsmath,allowspaces]{mathtools}
% Support for using RSFS (Raph Smith’s For­mal Script) fonts in maths
\usepackage{mathrsfs}
% LaTeX support file to use the WASY2 (Waldi Sym­bol) fonts
%\usepackage{wasysym}
% Typesetting theorems (AMS style)
\usepackage{amsthm}
% Place lines through maths formulae
\usepackage{cancel}
% Access bold symbols in maths mode
\usepackage{bm}

%================================================================
% Figures, Tables, Floats
%================================================================
% Improved interface for floating objects
\usepackage{float}
% Customising captions in floating environments
\usepackage{caption}
% Extending the array and tabular environments
\usepackage{array}
% Create tabular cells spanning multiple rows
\usepackage{multirow}
% Table heads with diagonal lines
\usepackage{diagbox}
% Enhanced support for graphics
\usepackage{graphicx}
% Add picture commands (or backgrounds) to every page
\usepackage{eso-pic}
% Figures broken into subfigures
\usepackage{subfig}
% Support for sub-captions : can't be used with the subfig package
%\usepackage{subcaption}
% Continuation headings and legends for floats
%\usepackage{ccaption}
% Publication quality tables in LaTeX
\usepackage{booktabs}
% Flexible LaTeX tabulars
\usepackage{tabu}

%================================================================
% Tikz
%================================================================
%\usepackage{tikz}
%\usepackage{tkz-graph}
%%\usetikzlibrary{arrows}
%\usetikzlibrary{arrows,positioning,shapes,backgrounds,fit}  

%================================================================
% Miscellaneous
%================================================================
% Easy access to the Lorem Ipsum dummy text
\usepackage{lipsum}
% Extension of the keyval (???) package
%\usepackage{xkeyval}
% Captions on more than floats. Note that the cap­tion pack­age in­cludes a \cap­tionof com­mand that is an ex­ten­sion of that pro­vided by this pack­age.
%\usepackage{capt-of}





